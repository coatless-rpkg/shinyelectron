% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-runtime.R
\name{embed_python_runtime}
\alias{embed_python_runtime}
\title{Embed a portable Python runtime into a bundled Electron build}
\usage{
embed_python_runtime(
  output_dir,
  packages,
  index_urls,
  version,
  platform,
  arch,
  verbose = TRUE
)
}
\arguments{
\item{output_dir}{Character. The Electron app output directory.}

\item{packages}{Character vector. Python package specs (may be empty/NULL).}

\item{index_urls}{Character vector. PyPI-like index URLs.}

\item{version}{Character. Resolved Python version (non-NULL from callers).}

\item{platform}{Character scalar. Target platform.}

\item{arch}{Character scalar. Target architecture.}

\item{verbose}{Logical. Whether to display progress.}
}
\value{
Invisibly, the path to the embedded \code{runtime/Python} directory.
}
\description{
Behavior-preserving extraction of the Python bundled-embedding block from
\code{\link[=build_electron_app]{build_electron_app()}}. ALWAYS installs + copies the interpreter so the shared
\code{runtime/Python} path exists for suite-wide bundled detection; only the pip
install is gated on a non-empty \code{packages} set. Warn-only (not abort) on pip
failure; the result is not verified, matching the original block. Reproduces
the three \code{output_dir}-derived paths and the unix-only fallback glob so the
\code{native-py.js} \code{sys.path} expectations hold.
}
\keyword{internal}
