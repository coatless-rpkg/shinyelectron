% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{fetch_published_sha256}
\alias{fetch_published_sha256}
\title{Fetch a published SHA-256 checksum for a portable runtime archive}
\usage{
fetch_published_sha256(checksum_url, asset_filename = NULL)
}
\arguments{
\item{checksum_url}{Character. URL of the \code{.sha256} sidecar or \code{SHA256SUMS}.}

\item{asset_filename}{Character or NULL. Archive file name to match within a
combined \code{SHA256SUMS}; \code{NULL} for a single-asset sidecar.}
}
\value{
Character SHA-256 hash, or \code{NULL}.
}
\description{
Reads a checksum file published alongside a runtime release and returns the
hash for one archive. Two layouts are supported, both in the standard
\code{sha256sum} format (\verb{<hash>  <filename>}):
}
\details{
\itemize{
\item Per-asset sidecar (portable R): the checksum file contains a single line
for the archive. Pass \code{asset_filename = NULL} and the first line's hash is
returned.
\item Combined \code{SHA256SUMS} (python-build-standalone): the file lists every
asset. Pass \code{asset_filename} and the matching line's hash is returned.
}

Returns \code{NULL} when the checksum cannot be fetched or no matching entry is
found, so callers can continue without verification rather than failing on a
transient network error (the same graceful-skip behavior as the Node.js
installer).
}
\keyword{internal}
