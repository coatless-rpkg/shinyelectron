% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/updates.R
\name{enable_auto_updates}
\alias{enable_auto_updates}
\title{Enable Auto-Updates}
\usage{
enable_auto_updates(
  appdir,
  provider = c("github", "s3", "generic"),
  owner = NULL,
  repo = NULL,
  check_on_startup = TRUE,
  auto_download = FALSE,
  auto_install = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{appdir}{Character path to app directory containing \verb{_shinyelectron.yml}}

\item{provider}{Character update provider: \code{"github"} (default), \code{"s3"}, or \code{"generic"}}

\item{owner}{Character GitHub username or organization (required for github provider)}

\item{repo}{Character GitHub repository name (required for github provider)}

\item{check_on_startup}{Logical whether to check for updates when app starts. Default \code{TRUE}.}

\item{auto_download}{Logical whether to download updates automatically. Default \code{FALSE}.}

\item{auto_install}{Logical whether to install updates automatically on quit. Default \code{FALSE}.}

\item{verbose}{Logical whether to show progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns the path to the updated config file.
}
\description{
Configures automatic update checking for your Electron application.
Updates are distributed via GitHub Releases, S3 buckets, or generic
HTTP servers.
}
\details{
Auto-updates require:
\enumerate{
\item A published application (e.g., to GitHub Releases)
\item Proper code signing for macOS and Windows (recommended)
\item The electron-updater package (automatically included in build)
}
\subsection{Update Providers}{

\strong{GitHub Releases} (recommended for open source):
\itemize{
\item Automatically detects new releases by comparing semver tags
\item Requires \code{owner} and \code{repo} parameters
\item Private repos require \code{GH_TOKEN} environment variable
}

\strong{S3 Bucket}:
\itemize{
\item For self-hosted updates behind a CDN
\item Configure bucket, region, and path in \verb{_shinyelectron.yml}
\item Bucket must allow public reads or use CloudFront signed URLs
\item Required bucket structure: \verb{/\{path\}/latest-mac.yml}, \code{latest-linux.yml}, \code{latest.yml}
}

\strong{Generic HTTP Server}:
\itemize{
\item For any HTTP server hosting update files
\item Configure base URL in \verb{_shinyelectron.yml}
\item Server must host \code{latest-mac.yml}, \code{latest-linux.yml}, \code{latest.yml} at the URL root
}
}

\subsection{Publishing Updates (GitHub Releases)}{

After enabling auto-updates, follow this workflow to publish updates:
\enumerate{
\item \strong{Bump version} in \verb{_shinyelectron.yml} (e.g., \code{version: "1.1.0"})
\item \strong{Rebuild} with \code{export(appdir, destdir, build = TRUE)}
\item \strong{Create a GitHub Release} with a semver tag matching the version:
\itemize{
\item Tag: \code{v1.1.0} (the \code{v} prefix is required)
\item Upload the built artifacts from \verb{destdir/electron-app/dist/}:
\itemize{
\item macOS: \code{.dmg} and \code{latest-mac.yml}
\item Windows: \code{.exe} installer and \code{latest.yml}
\item Linux: \code{.AppImage} and \code{latest-linux.yml}
}
}
\item The app checks for updates on startup (if \code{check_on_startup = TRUE})
and notifies the user when a new version is available
}
}

\subsection{Code Signing Requirement}{

macOS and Windows require code-signed builds for auto-updates to work.
Unsigned apps will fail the update verification step. Set
\code{signing: sign: true} in your config and provide credentials via
environment variables (see \code{?validate_signing_config}).
}
}
\examples{
\dontrun{
# Enable GitHub-based updates
enable_auto_updates(
  "path/to/app",
  provider = "github",
  owner = "myusername",
  repo = "myapp"
)

# Enable with automatic download
enable_auto_updates(
  "path/to/app",
  provider = "github",
  owner = "myorg",
  repo = "dashboard",
  auto_download = TRUE
)
}

}
\seealso{
\code{\link[=init_config]{init_config()}} for creating initial configuration
}
