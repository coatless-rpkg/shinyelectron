% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-python.R
\name{convert_py_to_shinylive}
\alias{convert_py_to_shinylive}
\title{Convert Python Shiny Application to Shinylive}
\usage{
convert_py_to_shinylive(appdir, output_dir, overwrite = FALSE, verbose = TRUE)
}
\arguments{
\item{appdir}{Character string. Path to the directory containing the Python Shiny application.}

\item{output_dir}{Character string. Path where the converted shinylive app will be saved.}

\item{overwrite}{Logical. Whether to overwrite existing output directory. Default is FALSE.}

\item{verbose}{Logical. Whether to display detailed progress information. Default is TRUE.}
}
\value{
Character string. Path to the converted shinylive application directory.
}
\description{
Converts a Python Shiny application directory into a shinylive application
that can run entirely in the browser using Pyodide.
}
\section{Details}{

This function converts a Python Shiny application to shinylive format using the
Python \code{shinylive} package. The application will run entirely in the browser
using Pyodide (Python compiled to WebAssembly).

\strong{Requirements:}
\itemize{
\item Python 3 must be available on the build machine
\item The Python \code{shinylive} package must be installed (\code{pip install shinylive})
\item The app directory must contain an \code{app.py} file
}
}

\examples{
\dontrun{
convert_py_to_shinylive(
  appdir = "path/to/python/shiny/app",
  output_dir = "path/to/shinylive/output"
)
}

}
