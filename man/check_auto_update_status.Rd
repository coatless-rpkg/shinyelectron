% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/updates.R
\name{check_auto_update_status}
\alias{check_auto_update_status}
\title{Check Auto-Update Status}
\usage{
check_auto_update_status(appdir)
}
\arguments{
\item{appdir}{Character path to app directory}
}
\value{
Invisibly returns the \code{updates} configuration list, which is always
present because \code{read_config()} deep-merges defaults. Elements include
\code{enabled} (logical, \code{FALSE} by default when auto-updates have never been
enabled), \code{provider} (character), \code{check_on_startup}, \code{auto_download},
\code{auto_install} (logical), and, for the GitHub provider, \code{github} (a list
with \code{owner}, \code{repo}, \code{private}).
}
\description{
Reports the current auto-update configuration status.
}
\examples{
# Check update configuration on a temporary app
app <- file.path(tempdir(), "check-updates-demo")
dir.create(app, showWarnings = FALSE)
writeLines("library(shiny)", file.path(app, "app.R"))
enable_auto_updates(app, owner = "myusername", repo = "myapp", verbose = FALSE)
check_auto_update_status(app)

}
