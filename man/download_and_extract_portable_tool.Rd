% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{download_and_extract_portable_tool}
\alias{download_and_extract_portable_tool}
\title{Download and extract a portable runtime into a cache directory}
\usage{
download_and_extract_portable_tool(
  label,
  version,
  install_path,
  download_url,
  executable_finder,
  force = FALSE,
  is_installed = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{label}{Character. Human-readable tool name for messages ("R", "Python").}

\item{version}{Character. Version string.}

\item{install_path}{Character. Target cache directory for the extracted
archive. Already-populated path is returned unless \code{force} is TRUE.}

\item{download_url}{Character. URL to the archive.}

\item{executable_finder}{Function with no arguments that returns the
path to the tool's executable after extraction, or NULL if not found.}

\item{force}{Logical. Reinstall even if \code{install_path} already exists.}

\item{is_installed}{Logical. Whether the runtime is already present.}

\item{verbose}{Logical. Whether to print progress messages.}
}
\value{
Invisibly returns the installation path.
}
\description{
Shared helper for \code{install_r()} and \code{install_python()}. Handles the
common flow: cache-hit short-circuit, download to temp file, extract
by archive type, verify the expected executable appears, cleanup.
}
\details{
\code{install_nodejs()} has additional requirements (SHA256 checksums,
directory renaming after extraction) and implements its own flow.
}
\keyword{internal}
