% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-python.R
\name{resolve_python_shinylive_cmd}
\alias{resolve_python_shinylive_cmd}
\title{Resolve a working shinylive Python CLI invocation}
\usage{
resolve_python_shinylive_cmd()
}
\value{
A list with \code{program} (character), \code{prefix} (character vector of
leading arguments), \code{label} (human-readable command), and \code{version}.
\verb{c(prefix, <cli args>)} are the arguments to pass to \code{program}.
}
\description{
Prefers the \code{shinylive} console script, but if that script is on PATH yet
backed by a Python without the module (a PATH / version skew that CI
environments hit), falls back to \verb{python -m shinylive}. Runs \code{--version} on
each candidate to confirm it actually executes -- an import check is not
enough, since shinylive ships no \verb{__main__.py}. Aborts with installation
hints if neither candidate works.
}
\keyword{internal}
