% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_electron_app}
\alias{run_electron_app}
\title{Run Electron Application for Testing}
\usage{
run_electron_app(app_dir, port = 3000, open_devtools = TRUE, verbose = TRUE)
}
\arguments{
\item{app_dir}{Character string. Path to the Electron application directory
(the \code{electron-app} subdirectory from \code{export()}).}

\item{port}{Integer. Port number for the development server. Default is 3000.}

\item{open_devtools}{Logical. Whether to open Chromium DevTools automatically. Default is TRUE.}

\item{verbose}{Logical. Whether to display detailed progress information. Default is TRUE.}
}
\value{
Invisibly returns the process object for the running application.
}
\description{
Launches a previously exported Electron application for testing and debugging
without building distributable packages. Pass the \code{electron-app} directory
from a prior \code{export()} call.
}
\section{Details}{

This function starts the Electron application for testing, which:
\itemize{
\item Opens the application in an Electron window
\item Optionally opens Chromium DevTools for debugging
\item Does NOT build distributable packages (use \code{export(build = TRUE)} for that)
}
}

\examples{
\dontrun{
# Run Electron app in development mode
run_electron_app("path/to/electron/app")

# Run with custom port and no dev tools
run_electron_app(
  app_dir = "path/to/app",
  port = 8080,
  open_devtools = FALSE
)
}

}
