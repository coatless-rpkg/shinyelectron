% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{nodejs_subprocess_env}
\alias{nodejs_subprocess_env}
\title{Environment for Node.js and npm child processes}
\usage{
nodejs_subprocess_env()
}
\value{
A character vector for the \code{env} argument of \code{\link[processx:run]{processx::run()}} and
\code{\link[=run_command_safe]{run_command_safe()}}, or \code{NULL} when Node.js is already on \code{PATH} or cannot
be resolved (the inherited environment is then used unchanged).
}
\description{
npm can be launched by its absolute path, but package lifecycle scripts
invoke \code{node} by name. When shinyelectron manages a Node.js install that is
not already on \code{PATH}, this makes that directory discoverable to npm and
every process it starts.
}
\details{
The result uses processx's special \code{"current"} entry so the child inherits
the full parent environment with \code{PATH} extended, rather than replacing it.
Replacing it would drop variables that npm and electron-builder depend on
(for example \code{APPDATA} and \code{LOCALAPPDATA} on Windows, or \code{HOME} elsewhere).
}
\keyword{internal}
