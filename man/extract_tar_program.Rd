% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{extract_tar_program}
\alias{extract_tar_program}
\title{Select the tar program used to unpack a \code{.tar.gz} runtime archive}
\usage{
extract_tar_program(
  os_type = .Platform$OS.type,
  system_root = Sys.getenv("SystemRoot", unset = "C:\\\\Windows")
)
}
\arguments{
\item{os_type}{Operating system family, defaulting to \code{.Platform$OS.type}.}

\item{system_root}{Windows system root, defaulting to the \code{SystemRoot}
environment variable.}
}
\value{
A value suitable for the \code{tar} argument of \code{\link[utils:untar]{utils::untar()}}: a path
to a tar executable, the string \code{"internal"}, or the result of
\code{\link[=Sys.which]{Sys.which()}}.
}
\description{
Windows ships bsdtar (libarchive) at \verb{System32\\\\tar.exe}. It handles the
PAX / long-name records in python-build-standalone archives that R's
internal tar cannot (those surface as "embedded nul in string" errors), and
unlike a GNU tar from Git for Windows it does not misparse \verb{C:\\\\...} paths as
remote hosts. Fall back to R's internal tar only when bsdtar is unavailable.
On macOS / Linux the system tar (bsdtar or GNU tar) already handles PAX
records, including the Apple \code{com.apple.cs.CodeSignature} xattrs in portable
R archives.
}
\keyword{internal}
