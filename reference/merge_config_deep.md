# Deep merge two lists

Recursively merges config into defaults, where config values override
defaults.

## Usage

``` r
merge_config_deep(defaults, config)
```

## Arguments

- defaults:

  List of default values

- config:

  List of config values to merge

## Value

Merged list
