# Check if a backend requires Express dependencies

Check if a backend requires Express dependencies

## Usage

``` r
backend_needs_express(backend_module)
```

## Arguments

- backend_module:

  Character string. The backend module filename.

## Value

Logical.
