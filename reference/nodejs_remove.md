# Remove Node.js installation

Remove Node.js installation

## Usage

``` r
nodejs_remove(version = NULL, verbose = TRUE)
```

## Arguments

- version:

  Character version to remove. If NULL, removes all versions.

- verbose:

  Logical whether to show progress. Default TRUE.

## Value

Invisible NULL
