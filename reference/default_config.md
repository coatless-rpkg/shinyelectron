# Get default configuration values

Returns the default configuration used when no config file exists or for
values not specified in the config file.

## Usage

``` r
default_config()
```

## Value

List of default configuration values
