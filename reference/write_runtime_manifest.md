# Write a runtime-manifest.json for the auto-download strategy

Write a runtime-manifest.json for the auto-download strategy

## Usage

``` r
write_runtime_manifest(
  app_dir,
  app_type,
  platform,
  arch,
  config,
  verbose = TRUE
)
```
