# Validate runtime strategy is compatible with app type

Validate runtime strategy is compatible with app type

## Usage

``` r
validate_runtime_strategy_for_app_type(strategy, app_type)
```

## Arguments

- strategy:

  Character string. The runtime strategy.

- app_type:

  Character string. The app type.
