# Copy the Dockerfile for the container strategy and bake in app dependencies

Copy the Dockerfile for the container strategy and bake in app
dependencies

## Usage

``` r
copy_and_bake_dockerfiles(output_dir, app_type, verbose = TRUE)
```
