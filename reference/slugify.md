# Convert a display name to a path-safe slug

Converts an application display name to a lowercase, hyphen-separated
string safe for use in file paths, container names, and npm package
names.

## Usage

``` r
slugify(name)
```

## Arguments

- name:

  Character string. The display name to slugify.

## Value

Character string. The slugified name.
