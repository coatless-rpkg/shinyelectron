# Get a default value

Retrieves a default value from SHINYELECTRON_DEFAULTS.

## Usage

``` r
get_default(key, default = NULL)
```

## Arguments

- key:

  Character name of the default to retrieve

- default:

  Value to return if key not found

## Value

The default value
